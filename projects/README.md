# Commerce Factory Projects

Each subfolder is an independent project repository managed via the mail bus. Clone or initialize new projects here following the pattern established in `glp1-companion/`.
